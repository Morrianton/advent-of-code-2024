/**
 * Multiplies two operands together.
 * @param {number} operand1 The first operand.
 * @param {number} operand2 The second operand.
 * @returns {number} The product of the operation.
 */
function mul(operand1, operand2) {
  return operand1 * operand2;
}

module.exports = { mul };
